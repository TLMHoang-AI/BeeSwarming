# Archive

This folder keeps legacy research artifacts that are no longer part of the primary experiment path.

Files are archived here rather than deleted so the original experimental history remains available for inspection. Exact duplicate notebooks are represented once in the reconstructed layout when possible.
